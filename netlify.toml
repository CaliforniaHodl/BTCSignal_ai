[build]
  publish = "public"
  command = "npm run build"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# NOTE: Schedules moved to GitHub Actions due to Netlify schedule bug
# See .github/workflows/ for cron schedules

